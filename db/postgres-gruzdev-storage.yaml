kind: PersistentVolume
apiVersion: v1
metadata:
  name: postgres-gruzdev-storage-volume
  labels:
    type: local
    app: postgres-gruzdev
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-gruzdev-storage-claim
  labels:
    app: postgres-gruzdev
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
